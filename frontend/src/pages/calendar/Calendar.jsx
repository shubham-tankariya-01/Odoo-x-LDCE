// pages/calendar/Calendar.jsx
// OWNER: Engineer 2 — build per frontend-engineer-2.md, Screen 11 (Global Calendar)
// Do not rename this file or move it. Do not change the export signature below.
// API calls this screen will need: GET /trips
import React from 'react';
import { AppShell } from '../../components/layout/AppShell';

export function Calendar() {
  return (
    <AppShell>
      <div style={{ padding: 'var(--space-8)' }}>Calendar — under construction (Engineer 2)</div>
    </AppShell>
  );
}
