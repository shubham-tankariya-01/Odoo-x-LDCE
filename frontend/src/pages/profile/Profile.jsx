// pages/profile/Profile.jsx
// OWNER: Engineer 2 — build per frontend-engineer-2.md, Screen 7 (User Profile)
// Do not rename this file or move it. Do not change the export signature below.
// API calls this screen will need: GET /users/me, PATCH /users/me
import React from 'react';
import { AppShell } from '../../components/layout/AppShell';

export function Profile() {
  return (
    <AppShell>
      <div style={{ padding: 'var(--space-8)' }}>Profile — under construction (Engineer 2)</div>
    </AppShell>
  );
}
