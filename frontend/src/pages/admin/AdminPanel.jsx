// pages/admin/AdminPanel.jsx
// OWNER: Engineer 2 — build per frontend-engineer-2.md, Screen 12 (Admin)
// Do not rename this file or move it. Do not change the export signature below.
// API calls this screen will need: GET /trips, DELETE /trips
import React from 'react';
import { AppShell } from '../../components/layout/AppShell';

export function AdminPanel() {
  return (
    <AppShell>
      <div style={{ padding: 'var(--space-8)' }}>Admin Panel — under construction (Engineer 2)</div>
    </AppShell>
  );
}
