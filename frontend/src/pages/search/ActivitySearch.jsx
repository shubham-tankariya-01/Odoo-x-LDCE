// pages/search/ActivitySearch.jsx
// OWNER: Engineer 2 — build per frontend-engineer-2.md, Screen 8 (Activity Search)
// Do not rename this file or move it. Do not change the export signature below.
// API calls this screen will need: GET /activities, GET /cities
import React from 'react';
import { AppShell } from '../../components/layout/AppShell';

export function ActivitySearch() {
  return (
    <AppShell>
      <div style={{ padding: 'var(--space-8)' }}>Activity Search — under construction (Engineer 2)</div>
    </AppShell>
  );
}
